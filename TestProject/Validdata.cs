using System;
using System.Data;
using System.Threading.Tasks;
using System.Text;
using NuGet.Frameworks;
using NUnit.Framework;
using System.Web.UI;
using NUnit.Framework.Api;
using System.IO;
using Newtonsoft.Json;
using Newtonsoft.Json.Linq;
using System.Net;
using OpenQA.Selenium.Remote;
using System.Collections.Generic;

namespace TestProject
{
    public class Validdata
    {
        //This url is given for example by me
        //we need url from 3rd party   
        //string apiurl = "http://commodityjson/json/";

        //Send webRequest
        //HttpWebRequest request = (HttpWebRequest)WebRequest.Create(apiurl);

        //Get the Web Response  
        //HttpWebResponse response = (HttpWebResponse)request.GetResponse();

        //Now, create the Stream
        //Stream responseStream = Response.GetResponseStream();



        //Seting Up the Stream Reader  
        //StreamReader readerStream = new StreamReader(responseStream, System.Text.Encoding.GetEncoding("utf-8"));

        //Get in a STring  
        //string json = readerStream.ReadToEnd();

        //Close the Stream  
        //readerStream.Close();
        //var jo = JObject.Parse(json);
        //Console.WriteLine("Country : "+(string) jo["Country"]);
        //Console.WriteLine("Commodity : " + (string) jo["Commodity"]);
        //Console.WriteLine("FixedOverhead: " + (decimal) jo["Fixed_Overhead"]);
        //Console.WriteLine("VariableOverhead : " + (decimal) jo["Variable_Overhead"]);
        //Console.WriteLine("TotalCost :"+(decimal) jo["TotalCost"]);
        //Console.WriteLine("OutputCostString :"+(string) jo["OutputCostString"]);

        //Console.ReadKey();
        
    }
}
